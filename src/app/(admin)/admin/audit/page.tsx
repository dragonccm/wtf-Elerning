import { prisma } from "@/lib/db";import { requireRole } from "@/lib/session";
export default async function AuditPage(){await requireRole("ADMIN");const logs=await prisma.auditLog.findMany({include:{actor:true},orderBy:{createdAt:"desc"},take:100});return <div><p className="text-sm font-bold text-[var(--md-primary)]">AN TOÀN HỆ THỐNG</p><h1 className="mt-1 text-3xl font-extrabold">Nhật ký hoạt động</h1><div className="md-table-wrap mt-6"><table className="md-table"><thead><tr><th>Thời gian</th><th>Người thực hiện</th><th>Hành động</th><th>Đối tượng</th></tr></thead><tbody>{logs.map(log=><tr key={log.id}><td>{log.createdAt.toLocaleString("vi-VN")}</td><td>{log.actor?.name??"Hệ thống"}</td><td><span className="md-chip">{log.action}</span></td><td>{log.entityType} · {log.entityId.slice(0,8)}</td></tr>)}</tbody></table></div></div>}
